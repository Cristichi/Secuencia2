<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.iesmurgi.cristichi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".game.WordGameActivity"></activity>
        <activity android:name=".game.SpecialGameActivity" />
        <activity android:name=".game.ImageGameActivity" />
        <activity android:name=".game.CharacterGameActivity" />
        <activity
            android:name=".TabbedMenuActivity"
            android:configChanges="orientation|keyboardHidden"
            android:label="@string/choose_gamemode"
            android:screenOrientation="portrait" />
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|keyboardHidden"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>